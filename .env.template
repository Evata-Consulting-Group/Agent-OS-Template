# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# NEVER commit real secrets. Committed files reference these via ${VAR}.

# ── LLM / API ──
ANTHROPIC_API_KEY=

# ── Google Workspace MCP (if used) ──
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# ── Messaging / comms (if used) ──
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ── Add your own below ──
